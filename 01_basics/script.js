const name = "simab"
let dob = 13
var lastName = "Shabbir"
ph = 9191
console.log(name, dob, lastName,ph)

//we can use var before declaration and it will store undefined value  by default

//ignore using var insted use let due to scoping issues
// we can define variables without any keyword but its not recommended

console.table([name,dob,lastName])